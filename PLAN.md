# 초기 Arrow Puzzle 플레이어블 프로토타입 계획

## Summary
- 빈 저장소에 `HTML + CSS + vanilla JS` 기반의 정적 브라우저 게임을 만든다.
- 첫 화면에서 바로 `Level 1`을 플레이할 수 있게 하고, 빌드 도구 없이 `index.html`만 열어도 동작하게 한다.
- 규칙은 “화살표를 누르면 해당 방향으로 직선 이동, 벽/다른 화살표에 막히면 실패, 보드 밖으로 나가면 제거”로 고정한다.

## Key Changes
- 파일 구조:
  - `index.html`: 게임 루트, HUD, canvas 포함
  - `src/styles.css`: 모바일 세로 화면 중심 UI, 흰색 퍼즐 카드, 하트/레벨 표시
  - `src/main.js`: 보드 렌더링, 입력 처리, 이동 판정, 레벨 클리어 처리
- 레벨 데이터 형식:
```js
{
  width: 9,
  height: 9,
  walls: [
    { x: 2, y: 1, side: "right" },
    { x: 4, y: 3, side: "bottom" }
  ],
  arrows: [
    { id: "a1", x: 1, y: 1, dir: "right" },
    { id: "a2", x: 6, y: 1, dir: "left" }
  ]
}
```
- 초기맵은 9x9 수동 제작 맵으로 넣는다. 최소 12개 이상의 화살표와 일부 내부 벽을 배치하고, 모든 화살표가 제거 가능한 해결 순서를 가진다.
- 조작:
  - 마우스 클릭 또는 터치로 화살표 선택
  - 이동 가능하면 해당 화살표가 보드 밖으로 애니메이션 후 제거
  - 이동 불가하면 짧은 흔들림/빨간 피드백만 표시
- 상태:
  - 남은 화살표 수가 0이면 `LEVEL CLEAR` 오버레이 표시
  - `Restart` 버튼으로 같은 레벨 초기화
  - 하트는 우선 장식 UI로 표시하고 실패해도 차감하지 않는다.

## Implementation Details
- `canvas`에 퍼즐을 직접 그린다.
  - 보드 크기는 화면 폭 기준으로 반응형 계산
  - 셀, 벽, 화살표를 좌표 기반으로 렌더링
  - DPR 보정으로 모바일에서도 선명하게 표시
- 이동 판정:
  - 선택한 화살표 위치에서 `dir` 방향으로 한 칸씩 전진 검사
  - 현재 칸의 진행 방향 벽이 있으면 실패
  - 다음 칸이 보드 밖이면 성공
  - 다음 칸에 다른 화살표가 있으면 실패
  - 다음 칸의 반대편 벽도 막힘으로 처리
- 애니메이션:
  - 성공 시 `requestAnimationFrame`으로 화살표를 보드 밖까지 이동
  - 애니메이션 중에는 추가 입력을 잠근다
  - 애니메이션 완료 후 레벨 상태에서 해당 화살표 제거

## Test Plan
- `index.html`을 브라우저에서 열었을 때 바로 Level 1이 보이는지 확인한다.
- 첫 맵에서 의도된 해결 순서대로 모든 화살표를 제거해 `LEVEL CLEAR`가 뜨는지 확인한다.
- 막힌 화살표를 눌렀을 때 제거되지 않고 실패 피드백만 나오는지 확인한다.
- 모바일 폭 약 360px, 데스크톱 폭 약 900px에서 보드와 HUD가 겹치지 않는지 확인한다.
- `node --check src/main.js`로 문법 검사를 한다.

## Assumptions
- 초기 버전은 앱스토어용이 아니라 브라우저에서 플레이 가능한 프로토타입으로 만든다.
- 이미지는 사용하지 않고 canvas 도형/텍스트로 구현한다.
- 자동 레벨 생성, 저장, 스테이지 선택, 광고, 하트 소모 시스템은 이번 범위에서 제외한다.
