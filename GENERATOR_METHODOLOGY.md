# 맵 생성 핵심 방법론 — 역설계 카빙(carving) 방식

> 최종 구현(`flow` 알고리즘)의 개념 정리. 코드는 `src/puzzle-generator.js` 참조.

## 1. 게임의 근본 성질

- 화살표는 "탈출(머리 방향으로 직선 이동해 보드 밖으로 나감)" 외에는 움직이지 않는다.
- 어떤 화살표를 제거하면 그 칸들이 **비워질 뿐**, 새로운 막힘은 절대 생기지 않는다.
- 따라서 "막힘" 관계는 **정적**이다: 화살표 A는 "처음에 A의 머리 광선 위에 놓여 있던
  화살표들"이 전부 제거되면 그때 제거 가능해진다.
- 결론: 보드가 풀린다 ⟺ (1) 자기 몸이 자기 머리를 막지 않고, (2) "B가 A를 막음"
  방향그래프에 **사이클이 없다**. → 사이클만 없으면 해는 반드시 존재한다.

## 2. 반드시 지킬 제약 (금지 사항)

이 게임은 **4방향** 퍼즐이고, 난이도는 **화살표가 멀리 가로질러 나가야** 생긴다.
그래서 아래 두 "지름길"은 **영구 금지**:

- **단일 그라디언트** — 모든 화살표를 2방향으로만 흐르게 함. 게임의 근원(4방향)을 회피.
- **사분면/섹터 드레인** — 각 화살표가 가장 가까운 가장자리로만 나감. 먼 가로지르기가
  없어 난이도가 성립하지 않음.

(수학적으로, 직선 광선이 항상 "오르막"이 되는 단일 스칼라 장은 평면뿐이고 그건 2방향만
가능 → 4방향을 보장된 무탐색 방식으로 얻는 건 불가능. 그래서 "구성으로 보장"이 아니라
"풀이 순서대로 짓는" 카빙을 쓴다.)

## 3. 카빙(역설계) 원리 — 사이클을 만들지 않고 짓기

보드를 **풀이를 거꾸로 시뮬레이션하듯** 깎아낸다.

- 화살표를 **제거 순서(O1, O2, …)** 대로 한 개씩 카빙한다.
- 새 화살표 Ok를 놓는 조건: 그 **머리 광선이 "이미 깎여 비어 있는 칸"(또는 보드 밖)만
  지나** 경계까지 도달해야 한다. = "O1…O(k-1)이 빠지고 나면 Ok가 나갈 수 있다."
- 이렇게 하면 모든 화살표가 놓이는 순간 유효한 탈출로를 보장받으므로, 완성된 보드의
  막힘 그래프는 **구조적으로 사이클이 없다**(탐색·검사·수리 불필요).
- **머리 방향·길이·모양에는 제약이 전혀 없다** → 4방향 자유.

핵심 효율 포인트: 머리가 "깨끗한 광선"을 가지는 칸은 **그 행/열에서 가장 바깥쪽
남은 칸**(행의 최우측 → 오른쪽 깨끗, 열의 최상단 → 위쪽 깨끗 …)뿐이다. 그래서 후보
머리는 행별/열별 "남은 칸" 집합에서 즉시 구한다.

## 4. 난이도 — 먼 탈출 우선

- 머리 광선이 **이미 깎인 칸들을 많이 가로지를수록** 그 화살표는 그 칸들의 주인 화살표에
  의존(나중에 제거)하고, 탈출 거리도 길다 → 긴 의존 체인 = 어려움.
- 그래서 후보 머리를 "경계까지의 거리(가로지르는 길이)"에 비례해 가중 선택한다.
- 거리 0(가장자리 바로 옆)인 후보만 초기 이동(처음부터 제거 가능)이 되므로, 가중치를
  주면 **초기 이동 수가 줄고** 체인이 깊어진다.

## 5. 모양 다양성 — 군집(타일링) 방지

그리디 카빙은 빈칸을 한 구역씩 쓸어나가므로, 몸통이 "깎인 경계에 달라붙는" 동일 모양을
반복 생성해 **같은 모양이 군집**되기 쉽다. 이를 깨기 위해:

- **꺾임 우선 성장**: 몸통을 늘릴 때 직진보다 방향 전환을 선호 → 2회 이상 꺾임·U턴 발생.
- **무작위 회전**: 경계에 달라붙는(저차수 우선) 결정론적 선택을 대부분 무작위로 대체.
- **공간 분산 선택**: 머리를 한 구역에 몰아 깎지 않고 보드 전역에 흩뿌려 깎음 → 인접
  화살표가 서로 다른 맥락에서 생겨 동일 모양 타일링이 깨짐.
- **긴 스네이크 우선**: 대부분 길게(짧은 건 가끔) → 길수록 각자 고유한 모양.
- **머리 방향 분산**: 최근 같은 방향을 많이 썼으면 가중치를 낮춰 U/D/L/R을 고르게.

조율 파라미터(개념): 꺾임/직진 비율, 무작위 vs 경계붙기 비율, 공간 분산 확률, 길이 범위,
먼-탈출 가중 지수, 방향 분산 강도.

## 6. 부분 커버 허용

- 100% 채울 필요 없다. 더 이상 유효하게 깎을 수 없는 고립 칸은 인접 화살표 꼬리에
  흡수하거나 **그냥 빈칸으로 둔다**. (이게 큰 맵에서 막혀 실패하던 문제를 없앰.)
- 빈칸이 생겨도 해의 존재·검증에는 영향이 없다(빈칸은 그냥 비어 있는 격자점).

## 7. 검증 (보장과 비보장)

- 매 레벨: 기하 검증(범위·활성점·중복) + **알려진 해 재생 검증**(카빙 순서가 실제
  규칙엔진에서 끝까지 제거되는지). 카빙은 구조적으로 해를 보장하지만, 이 검증을 안전망으로
  항상 돌려 실제 실패를 숨기지 않는다(오류는 드러낸다).
- 난이도 지표(초기 이동 수·분기·의존 깊이)는 막힘 DAG에서 O(V+E)로 직접 계산
  (무거운 지수적 솔버 미사용). 큰 맵 유일해는 목표 아님.

## 8. 성능

- 전 과정이 보드 크기에 거의 선형. 100×100도 수 초 내 생성, 사이클·백트래킹 없음.

## 9. (개정) 먼 탈출 강화 — 구조적 원인과 점수 규칙

### 구조적 원인 (`collectHeadCandidates()` 조사 결과)

후보 머리는 각 행/열의 **극단 셀**에서만 나오고, 방향이 **고정**되어 있다:

- 각 행의 남은 셀 중 **rowMax → 무조건 right**
- 각 행의 **rowMin → 무조건 left**
- 각 열의 **colMax → 무조건 down**
- 각 열의 **colMin → 무조건 up**

즉 모든 후보 머리는 "자기가 극단인 그 변" 바깥쪽만 향한다. 카빙이 바깥에서 안으로
벗겨지므로(peeling) 셀은 보통 **가까운 변** 쪽에서 먼저 극단이 되고, 그 가까운 변으로
향하는 후보로 깎인다. → 결과적으로 **사분면 드레인**(가까운 외곽으로 즉시 탈출).
실측: 가장 가까운 외곽 1위 ≈ 69%, 1~2위 ≈ 95%. (먼 탈출 요구 미달 — 감점 인정.)

### 점수 규칙 (개정 방향)

후보 머리를 고를 때 다음 가산점을 적용해 **먼 변으로 향하도록** 유도한다.

1. **외곽으로 바로 빠져나가지 않는 것에 가산점** — 가까운 변으로 즉시 탈출하는 후보를 억제.
2. **가장 먼 외곽으로 향하면 +2, 두 번째로 먼 외곽으로 향하면 +1** — 머리 방향을
   먼 변 우선으로 가중.

구현 함의(주의): 위 점수는 "그 순간 존재하는 후보" 중에서만 작동한다.

## 10. 채택: 방식 A (자유 방향 + 시뮬레이션 검증) + 점진 확장

- **방향을 고정하지 않는다.** 한 셀이 여러 변으로 깨끗한 광선을 가질 수 있을 때
  (또는 자유 배치에서) **먼 변을 향하는 방향을 우선** 선택한다. (9절의 +2/+1 점수)
- **검증은 시뮬레이션으로.** "이 화살표가 (이미 치워진 것 기준) 실제로 빠져나갈 수
  있는가"를 광선이 비어 있는지로 확인 = 풀이 한 수 시뮬레이션. 이로써 데드락(사이클)
  없음을 보장/확인한다.
- **구불구불 가중치 더 낮춤.** 몸통 꺾임 선호를 낮추고 직진 비중을 올린다(과한 미로화 방지).
- **점진 확장(맵 크기 비례).** 맵이 커질수록 긴 화살표 비중·최대 길이를 키운다
  (예: 최대 길이를 보드 변 길이에 비례해 확대). 큰 맵일수록 멀리 가로지르는 hero
  화살표가 더 자주 나오게.

### 솔직한 한계(천장)

먼 탈출 화살표는 본질적으로 **나중에 제거되는 화살표**다(그 앞 통로가 먼저 비워져야
하므로). 또한 같은 줄에서 서로 반대로 멀리 뻗는 두 화살표는 항상 사이클이다. 따라서
"**대부분**의 화살표를 4방향 + 먼 탈출 + 무사이클"로 만드는 것은 불가능에 가깝다.
현실 목표는 **먼 탈출 비율을 의미 있게 끌어올리는 것**이며, 성공 기준은 절대 거리가
아니라 **머리 방향의 가까운-외곽 비율(1위/1~2위)** 로 측정·검증한다.

### 측정으로 드러난 사실 (점수만으로는 부족)

먼-변 점수(+2/+1)는 작은/중간 맵에선 잘 먹혔지만(20×20 가까운1위 69%→28%) 큰 맵에선
거의 효과 없음(예: 가까운1위 ~64%). 이유: 큰 맵에선 먼 변까지의 통로가 잘 안 열려
**먼-후보 자체가 드물게 생기기** 때문. → 큰 맵엔 통로를 **의도적으로** 비우는 장치 필요.

## 11. 코리더(스파인) 설계 — 먼 탈출을 의도적으로 만들기

핵심: 먼 탈출 hero 화살표를 위해 그 **탈출 직선(코리더)** 을 미리 비워둔다. 카빙은
제거 순서대로 깎으므로, "hero를 늦게 제거되게(=나중에 카빙) 두고, 그 앞 통로 칸들을
먼저 비우면" hero의 광선이 먼 변까지 뚫린다.

**구현 방식 — 스파인 예약 + 2단계 카빙**

1. **스파인 생성(예약)**: 길고 곧은 직선 세그먼트를 K개 뽑는다. 각 스파인은 머리가
   **먼 변**을 향하고(머리에서 그 변까지 거리가 큼), 몸통은 반대쪽으로 뻗는다. 방향
   d는 4방향 무작위 → 4방향 먼 탈출 확보. 이 칸들을 `reserved`(예약)로 표시한다.
   K는 맵 크기에 비례(점진 확장).
2. **1단계(일반 카빙)**: 예약 안 된 칸들을 기존 방식으로 깎는다. 단, 일반 화살표의
   광선은 **남은 칸 + 예약 칸 모두를 장애물로** 본다(예약 스파인은 hero가 빠지기 전엔
   보드에 남아 있으므로). 즉 일반 화살표는 스파인을 관통해 나가지 못한다.
3. **2단계(스파인 카빙)**: 1단계가 끝나 스파인 앞이 비면, 머리 광선이 깨끗한 스파인을
   골라 **통째로 하나의 긴 화살표**로 깎는다(먼 변으로 탈출). 서로 막는 스파인은 광선이
   열리는 순서대로 처리하고, 끝내 안 열리는 스파인은 버린다(빈칸 허용).

**보장**: 모든 화살표가 "광선이 (그때 남아 있는 것들 기준) 깨끗할 때만" 카빙되므로
무사이클은 그대로 유지된다. 스파인 = 나중에 제거되는 먼 탈출 화살표.

**성능**: 100×100은 더 이상 생성하지 않는다(상한 ~60×60). 그래서 광선 명시 검사
(예약 칸 포함)를 써도 비용은 충분히 작다.

**조율**: 스파인 개수/밀도, 스파인 길이, 머리의 먼-변 목표 거리.

### 측정 결과 — 코리더는 실패 (기본 비활성)

다중 시드 평균(8회)으로 검증한 결과 스파인은 **역효과**였다:

- 20×20: 가까운1위 39.3% → 35.3% (미미)
- 40×40: 가까운1위 44.3% → **57.0% (오히려 악화)**, 빈칸도 증가

원인(구조적): 예약된 스파인은 자기 줄만 막는 게 아니라 **그 줄을 가로지르려는 다른
모든 화살표의 탈출을 막는다**(가로 스파인이 그 열 구간의 세로 탈출을 전멸시킴). 스파인은
소수인데 다수를 오염시키므로, 가까운-탈출 비율이 **올라가고** 빈칸이 늘어난다.

→ **스파인은 기본 비활성(`flowSpineDensity` = 0)**. 코드는 실험용으로만 남긴다.

### 실제로 효과 있었던 것 (채택)

먼-변 점수(+2/+1) + 구불구불 가중치 낮춤(직진↑) 조합이 진짜 개선을 냈다:
**40×40 가까운1위 ≈ 69% → ≈ 44%** (다중 시드 평균), 빈칸 ~2%. 작은 맵은 더 큼
(20×20 ≈ 39%). 이게 현재 채택된 설정이며, 100×100은 생성하지 않는다(상한 ~60×60).

---

## 표시(캔버스) 관련 메모

- 점 간격을 **10×10 기준 밀도로 고정**하고, 보드가 크면 캔버스를 키워(스크롤) 비율을 유지.
  점이 많아도 오밀조밀해지지 않음. (`src/main.js` calculateView, `src/styles.css` board-stage)
