# 작업 규칙

아래 규칙을 매 작업 전에 참조한다.

1. 아무리 사소한 부분이라도 사용자의 허가를 받은 뒤 수정한다.
2. 시각적인 부분은 사용자만 판단한다. Codex는 스크린샷, 상태, 로그를 제시하고 판단을 기다린다.
3. 중복되는 함수나 로직이 있으면 공통 헬퍼로 빼는 방향을 먼저 제안하고, 허가 후 정리한다.
4. `fallback`, `try/catch` 등으로 오류를 숨기지 않는다. 오류는 로그나 콘솔 상태로 드러나게 해서 디버깅이 쉽도록 한다.
5. 모르는 부분은 추정하지 않는다. 사용자에게 물어보거나 로그/데이터로 확인한다.
6. 시각 판단 표현인 "좋아 보인다", "느리다", "빠르다", "적당하다" 같은 결론은 사용자 확인 없이 내리지 않는다.
7. 이미 허가된 작업 범위를 벗어나는 추가 구현, 배치 변경, 수치 조정, 색상 변경, 리팩터링은 진행하지 않는다.
8. 새로 함수를 만들기 전 비슷한 역할의 함수가 있는지 검색해서, 역할이 중복되면 3번 공통헬퍼를 만든다.
9. 시각적 디버깅이 필요한 부분은 반드시 사용자가 검증한다. '퍼즐제작에 실패한다 등'
10. 상수 하드코딩을 금지하고 상수파일은 공통으로 모은다.
11. 작성한 계획파일은 반드시 프로젝트 루트에 저장한다