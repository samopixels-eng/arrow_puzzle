# 퍼즐 제작 로직 요약

## 핵심 목표

활성 점(`mask`가 있으면 마스크 내부 점, 없으면 전체 격자)을 화살표 경로로 모두 덮고, 실제 플레이에서 제거 가능한 순서(`solutionOrder`)를 가진 레벨을 만든다.

큰 맵에서는 "불가능" 판정을 코드가 단정하지 않는다. 실패 프리뷰는 제너레이터 탐색 실패를 보여주는 디버그 상태이며, 최종 시각 판단은 사용자가 한다.

## 공통 구조

- 점은 `"x,y"` 키로 관리한다.
- 화살표는 직교 경로 `path`를 가진다.
- 후보 화살표는 아직 비어 있는 점(`freeKeys`)만 사용하므로 기본적으로 다른 화살표와 점 충돌하지 않는다.
- `validateGeometry()`는 최종적으로 범위, 활성점, 자기교차, 점/엣지 중복, full-cover를 검사한다.
- `validateKnownSolution()`은 주어진 제거 순서가 실제로 replay 가능한지만 선형으로 검사한다.

## 역설계 원리

일반 레벨은 플레이 순서대로 만들지 않고, 제거 순서의 역순으로 만든다.

목표 풀이가 `A1 -> A2 -> ... -> AN`이라면 제너레이터는 `AN, ..., A2, A1` 순서로 배치한다.

- 배치 중인 새 화살표 `Ai`는 이미 놓인 화살표보다 먼저 제거될 화살표다.
- 이미 놓인 화살표들은 `Ai`보다 나중에 제거될 화살표다.
- 새 화살표가 기존 화살표의 탈출 레이를 막으면, 시작 상태에서 기존 화살표는 막힌다.
- 이후 `Ai`가 먼저 제거되면 막혀 있던 기존 화살표가 열릴 수 있다.

이 방식으로 풀이 존재를 설계한다. 즉, 무작위로 보드를 만든 뒤 풀 수 있는지 찾는 게 아니라, 풀 수 있는 순서를 거꾸로 만들면서 보드를 채운다.

## 첫 번째 화살표 로직

단일 패스 생성에서 실제 플레이의 첫 번째 화살표는 "마지막에 배치된 화살표"다.

이유:

- 배치 배열 `arrows`는 역설계 순서로 쌓인다.
- 생성 완료 후 `solutionOrder = arrows.map(id).reverse()`를 만든다.
- 따라서 `solutionOrder[0]`은 마지막에 배치된 화살표다.

단일 패스에서는 마지막 배치 후보에 대해 다음을 요구한다.

- `freeAfter.size === 0`이면, 즉 마지막 남은 점까지 덮는 후보라면 외곽으로 나가는 방향이어야 한다.
- `isOuterExit()`가 참이어야 한다.
- `buildArrowCandidate()` 단계에서 현재 보드 기준으로 제거 가능해야 한다.

그래서 단일 패스 레벨은 첫 번째 제거 화살표가 구조적으로 확보된다.

스티칭 생성에서는 이 규칙을 다르게 다룬다.

- 타일 스티칭 중에는 후보가 즉시 제거 가능해야 한다는 조건을 강제하지 않는다.
- 로컬 역순(`constructedOrder`)도 권위 있는 풀이로 쓰지 않는다.
- 전체 배치 후 `buildGlobalSolutionOrder()`가 현재 보드에서 실제 removable 화살표를 찾아 첫 번째 제거 후보를 결정한다.

즉, 단일 패스의 첫 화살표는 역설계로 보장하고, 큰 맵 스티칭의 첫 화살표는 전역 스케줄러가 실제 보드에서 찾는다.

## 단일 패스 생성

1. 전체 활성 점을 `freeKeys`로 시작한다.
2. 남은 자유 컴포넌트 중 후보 경로를 만든다.
3. 긴 선 쿼터(`longLineQuota`)가 남아 있으면 긴 화살표를 우선 시도한다.
4. `growPath()`가 제한 DFS로 목표 길이까지 경로를 찾는다.
5. 후보가 남은 점을 고립점 1개로 만들면 버린다.
6. 후보 방향을 정하고 단일 화살표 geometry를 검사한다.
7. 점수(`scoreInsertion`)가 가장 좋은 후보를 채택한다.
8. 모든 점을 덮으면 배치 역순을 `solutionOrder`로 삼고 replay 검증한다.

## 후보 점수

후보 점수는 대략 다음 요소를 합산한다.

- 기존 removable 화살표를 새 후보가 막은 수(`blockedGain`)
- 후보 몸통이 기존 탈출 레이를 가로지르는 수
- 레이 교차 대상 화살표 수
- 교차 위치와 후보 길이 가중치
- 꺾임 수
- 화살표 길이
- 긴 선 목표 달성 보너스

즉, 단순히 "몇 개 막았나"뿐 아니라 "어떤 탈출 레이를 얼마나 의미 있게 가로지르는가"를 반영한다.

## 큰 맵 스티칭

큰 맵은 `tileWidth`/`tileHeight` 기준으로 타일을 나누지만, 타일을 완전히 독립 생성해서 붙이지 않는다.

현재 방식:

1. 타일 목록을 만든다.
2. 각 타일의 활성 점만 `freeKeys`로 잡는다.
3. 실제 후보 검사는 글로벌 `levelShell` 위에서 수행한다.
4. 스티칭 경로에서는 후보가 즉시 제거 가능해야 한다는 조건을 강제하지 않는다.
5. 로컬 삽입 역순(`constructedOrder`)은 디버그 정보로만 남긴다.
6. 전체 배치 후 `buildGlobalSolutionOrder()`가 현재 보드에서 removable 화살표를 반복 제거해 실제 전역 풀이 순서를 만든다.
7. 전역 스케줄러가 막히면 실패 후보로 처리한다.

중요: `tile-candidate-missing`은 "퍼즐이 불가능하다"는 뜻이 아니다. 제너레이터가 현재 탐색 조건 안에서 다음 후보를 못 찾았다는 뜻이다.

## 난이도 분석

- 기본 난이도 분석은 `analyzeLevel()`로 선형 replay 기반 지표만 계산한다.
- 작은 맵은 `fullSolveMaxCells` 이하일 때만 지수적 `solveLevel()`을 최종 1회 돌려 solution count를 계산한다.
- 큰 맵은 uniqueness 보장을 목표로 하지 않는다. 풀이 존재와 구조 지표가 우선이다.

## 디버그 프리뷰

`generateDebugPreviewLevel()`은 스티칭 실패 시 마지막 실패 상태를 레벨처럼 반환한다.

프리뷰에 포함되는 정보:

- 이미 배치된 화살표
- 실패 이유(`reason`)
- 실패 타일 좌표/크기
- 아직 덮지 못한 점(`freeKeys`)
- 배치된 화살표 수와 남은 점 수

DEV 모드에서는 `debug-stitch-15` 스테이지로 볼 수 있다.

화면 표시:

- 실패 타일: 붉은 반투명 영역
- 남은 점: 붉은 점
- 하단 표시: `Debug: N placed / M open`

## 보장과 비보장

보장하려는 것:

- 활성 점 밖에 화살표를 놓지 않음
- 점/엣지 중복 없음
- 작은/일반 레벨은 known solution replay 통과
- 큰 맵은 전역 스케줄러가 찾은 제거 순서만 solution으로 사용

보장하지 않는 것:

- 큰 맵 uniqueness
- 제너레이터 실패가 실제 불가능성이라는 결론
- 시각적 품질의 자동 판정

시각적 디버깅과 품질 판단은 사용자가 최종 판단한다.
